# Nemex-mining-app

A cryptocurrency mining platform for NMX tokens.

## Setup
1. Run `npm install`
2. Configure environment variables
3. Run `npm start`

## Structure
- `frontend/` - Client-side files
- `backend/` - Server-side API
- `sections/` - Dynamic content sections